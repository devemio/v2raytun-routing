# v2raytun-routing

Небольшая CLI-утилита на Go для генерации **import_route** ссылки (`v2rayTun://`), которую можно напрямую импортировать в **v2RayTun** / совместимые клиенты V2Ray.

Проект преобразует список доменов в JSON‑маршрут и кодирует его в Base64‑ссылку формата:
```
v2rayTun://import_route/<base64>
```

## Возможности

- 📄 Чтение доменов из текстового файла
- 🧹 Нормализация доменов:
  - удаление `http://`, `https://`, `www.`
  - приведение к нижнему регистру
  - удаление комментариев и пустых строк
- 🔁 Удаление дубликатов
- 🆔 Автоматическая генерация UUID
- 📦 Готовая ссылка для импорта маршрута

## Формат входного файла

Одна строка — один домен.

Поддерживаются комментарии и произвольный формат:

```text
# комментарий
google.com
https://example.org
www.test.com   # inline comment
```

## Использование

```bash
go run . domains.txt
```

Пример вывода:

```text
v2rayTun://import_route/eyJuYW1lIjoiRGVmYXVsdCIsIC4uLn0=
```

Скопируйте ссылку целиком и импортируйте её в **v2RayTun**.

## Что создаётся

Генерируется маршрут со следующими параметрами:

- `DomainStrategy`: `AsIs`
- `DomainMatcher`: `hybrid`
- `OutboundTag`: `direct`
- Один rule типа `field` со списком доменов

## Структура маршрута (упрощённо)

```json
{
  "name": "Default",
  "domainStrategy": "AsIs",
  "domainMatcher": "hybrid",
  "rules": [
    {
      "type": "field",
      "domain": ["example.com"],
      "outboundTag": "direct"
    }
  ]
}
```

## Лицензия

MIT
